pre-push:
  parallel: true
  jobs:
    - run: npm run check -- --no-errors-on-unmatched "{staged_files}"

pre-commit:
  parallel: true
  jobs:
    - run: npm run fix -- --no-errors-on-unmatched "{staged_files}"
      stage_fixed: true
